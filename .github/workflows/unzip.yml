name: Unzip Manager

on:
  workflow_dispatch: # Активируем возможность ручного запуска
    inputs:
      zip_file: # Поле ввода для выбора ZIP файла
        description: "Enter the name of the ZIP file (e.g., archive.zip)" 
        required: true # Поле обязательно для заполнения
        default: ""    # Значение по умолчанию пустое

jobs:
  unzip:
    runs-on: ubuntu-latest # Работает на последней версии Ubuntu
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Find ZIP File
        id: search_zip
        run: |
          # Поиск файла
